{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://sidetours.excelia.com/schemas/compass/compass.schema.json",
  "title": "Compass configuration",
  "description": "Schema for compass.yaml at the root of a host project. Loaded by Sidetours\\Compass\\Engine\\Configuration::load() at the start of every `compass check`, `compass baseline`, and `compass prompts` invocation.",
  "type": "object",
  "additionalProperties": false,
  "required": ["paths", "rules"],
  "properties": {
    "paths": {
      "type": "array",
      "title": "Scan paths",
      "description": "Directories or files Compass walks for *.php sources. Entries are resolved relative to the project root (the directory the CLI was launched from); absolute paths are accepted as-is.",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "examples": [["src", "tests"]]
    },
    "exclude": {
      "type": "array",
      "title": "Exclude globs",
      "description": "Path globs to skip during the file walk. Supports '*' (single segment), '**' (recursive), and '?' (single char). Plain strings without glob characters are matched as directory prefixes.",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "examples": [["src/Generated", "tests/Fixtures/**"]]
    },
    "rules": {
      "type": "array",
      "title": "Activated rules",
      "description": "List of rule identifiers Compass should run. Each entry is either a built-in short name (matches Rule::name() of a class shipped with the package) or a fully-qualified PHP class name of a custom rule that implements Sidetours\\Compass\\Rules\\Rule. Custom-rule entries MUST contain a backslash so the loader can disambiguate them from built-in names.",
      "minItems": 1,
      "items": {
        "oneOf": [
          {
            "type": "string",
            "title": "Built-in rule",
            "description": "Short name of a rule shipped with Compass.",
            "enum": [
              "named-arguments",
              "named-method-arguments",
              "promoted-properties"
            ]
          },
          {
            "type": "string",
            "title": "Custom rule (PHP FQCN)",
            "description": "Fully-qualified class name of a class implementing Sidetours\\Compass\\Rules\\Rule. Must contain a backslash.",
            "pattern": "^[A-Za-z_][A-Za-z0-9_]*(\\\\[A-Za-z_][A-Za-z0-9_]*)+$",
            "examples": [
              "App\\Compass\\NoFinalKeyword",
              "Acme\\Architecture\\Rules\\NoCyclicDependencies"
            ]
          }
        ]
      }
    },
    "ignore": {
      "type": "object",
      "title": "Glob-based suppressions",
      "description": "Map of file glob => list of rule short names to suppress for matching files. Use ['*'] to silence every Compass rule for that glob. Glob syntax is the same as 'exclude'. Suppressions complement, but are independent of, the baseline file.",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1
        },
        "minItems": 1
      },
      "examples": [
        {
          "src/Legacy/**": ["*"],
          "tests/**": ["named-method-arguments"]
        }
      ]
    },
    "baseline": {
      "type": "string",
      "title": "Baseline file path",
      "description": "Path (relative to project root, or absolute) of an auto-generated PHP file holding violation fingerprints to suppress. Pre-existing violations recorded there stay quietly tracked; new ones still fail CI. Generated by `compass baseline`. Omit to disable baselining.",
      "examples": ["compass-baseline.php"]
    }
  }
}
